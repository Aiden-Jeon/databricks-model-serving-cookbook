[project]
name = "churn-preproc"
version = "0.1.0"
description = "Customer churn preprocessing utilities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
]

[build-system]
requires = ["uv_build>=0.5.0,<0.12"]
build-backend = "uv_build"
